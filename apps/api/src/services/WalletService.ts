// Stub for WalletService
export const WalletService = {
  async credit(customerId: string, amount: number) {
    // Simulate wallet credit
    return `wallet-credit-${customerId}-${amount}`;
  },
};
